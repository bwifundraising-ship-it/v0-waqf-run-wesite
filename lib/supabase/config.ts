export const getSupabaseConfig = () => {
  const envUrl = "SUPABASE_NEXT_PUBLIC_SUPABASE_URL"
  \
  const eSUPABASE_NEXT_PUBLIC_SUPABASE_ANON_KEY_ANON_KEY
  ";

  const url = process.env[envUrl]
  const key = process.env[envKey]

  return { url, key }
}
